# =====================================================
# HHG-SPA POS — Environment Template
# 複製為 .env.local 並填入實際值
# =====================================================

# --- Supabase ---
NEXT_PUBLIC_SUPABASE_URL=https://YOUR_PROJECT.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_xxx
SUPABASE_SECRET_KEY=sb_secret_xxx

# Supabase CLI / Management
SUPABASE_PROJECT_REF=YOUR_PROJECT_REF
SUPABASE_DB_PASSWORD=YOUR_DB_PASSWORD
SUPABASE_ACCESS_TOKEN=sbp_xxx

# --- Acumatica ERP ---
ACUMATICA_BASE_URL=https://your-instance.acumatica.com
ACUMATICA_COMPANY=YourCompany
ACUMATICA_BRANCH=HSPA2
ACUMATICA_LEDGER_ID=MGMT
ACUMATICA_API_VERSION=23.200.001
ACUMATICA_SERVICE_USERNAME=service_account
ACUMATICA_SERVICE_PASSWORD=service_password

# --- Session ---
# Auth is on Supabase Auth — session signing is handled by Supabase itself
# (using the project's JWT secret server-side, no client-supplied key needed).
# The legacy SESSION_SECRET (signed our old custom JWT) is no longer used; if
# you're migrating an old .env.local, you can delete that line.

# Dev override (optional): set to "true" to skip Supabase Auth in local dev
# and act as the seeded admin. Never enable in any deployed environment.
# AUTH_BYPASS=true

# --- App Config ---
NEXT_PUBLIC_APP_NAME=HHG-SPA POS
NEXT_PUBLIC_APP_VERSION=0.1.0
